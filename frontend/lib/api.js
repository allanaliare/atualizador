export async function api(path,options={}){const token=localStorage.getItem('token');const r=await fetch(`/api/v1${path}`,{...options,headers:{...(options.body instanceof FormData?{}:{'Content-Type':'application/json'}),Authorization:`Bearer ${token}`,...options.headers}});if(r.status===401){localStorage.removeItem('token');location.href='/';throw new Error('Sessão expirada')}if(!r.ok){const data=await r.json().catch(()=>({}));throw new Error(data.error||'Falha na requisição')}return r.json()}
